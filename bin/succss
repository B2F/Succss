#!/usr/bin/env bash
script=`realpath "$0"`
scriptDir="$(dirname $script)"
command=$1
if [ -n "$2" ]; then
  userFile=$PWD/$2
fi
shift
shift
casperjs test $scriptDir/../succss-bridge.js --do="$command" --dataFile="$userFile" $@

realpath() {
    [[ $1 = /* ]] && echo "$1" || echo "$PWD/${1#./}"
}
